export const hasPermission = (_module: string, _action: string): boolean => {
    // const permissions = JSON.parse(localStorage.getItem(`${module}Permission`)) || [];
    // return permissions.includes(action);
    return true;
};
